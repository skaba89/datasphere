-- GuineaTender AI — Migration initiale de référence (baseline)
-- Cette migration est marquée comme "already applied" sur les bases existantes.
-- Elle ne sera JAMAIS exécutée — elle sert uniquement de point de référence
-- pour l'historique des migrations Prisma.

-- NOTE: Ne pas modifier ce fichier. Toute nouvelle modification du schéma
-- doit passer par "npx prisma migrate dev --name <description>" qui créera
-- une nouvelle migration incrémentale.

SELECT 1;
